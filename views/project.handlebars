<div class="text-center">
  <h2>{{name}}</h2>
  <h3>{{description}}</h3>
  {{!-- <p>Created by {{user.name}} on {{format_date date_created}}</p> --}}
  <p>Created by {{user.name}} on {{format_date date_created}}</p>

  <p>
    <span class="fund">Post Number {{ needed_funding}}</span>
   
  </p>
  <p>
    <button class="btn btn-primary">Comment</button>
  </p>
</div>
